import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // Placeholder test — the app requires async initialization (SharedPreferences)
    // which needs additional setup for widget tests.
    expect(true, isTrue);
  });
}
